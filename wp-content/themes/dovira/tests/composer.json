{
	"description": "Dev-only test tooling for the dovira theme. Never deployed: this directory's vendor/ is gitignored. The theme's own composer.json holds runtime packages only, because the theme's vendor/ is committed (docs/DECISIONS.md, 2026-09-23).",
	"require-dev": {
		"brain/monkey": "^2.7",
		"phpunit/phpunit": "^12.5"
	},
	"autoload-dev": {
		"psr-4": {
			"dovira\\Tests\\": "./"
		}
	},
	"config": {
		"platform": {
			"php": "8.3"
		},
		"sort-packages": true
	},
	"scripts": {
		"test": "phpunit"
	}
}
